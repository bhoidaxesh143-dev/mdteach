<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Exam Results Report</title>

    <style>
        body {
            font-family: DejaVu Sans, sans-serif;
            font-size: 12px;
            color: #1e293b;
        }

        .header {
            text-align: center;
            margin-bottom: 25px;
        }

        .header h1 {
            margin: 0;
            font-size: 24px;
            color: #0f172a;
        }

        .header p {
            margin: 4px 0;
            color: #64748b;
            font-size: 11px;
        }

        .stats {
            margin-bottom: 20px;
            width: 100%;
        }

        .stats td {
            padding: 10px;
            border: 1px solid #cbd5e1;
            text-align: center;
            font-weight: bold;
        }

        .stats-label {
            font-size: 10px;
            color: #64748b;
            display: block;
            margin-bottom: 4px;
        }

        table.results {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }

        table.results th {
            background: #1e293b;
            color: white;
            padding: 10px;
            font-size: 11px;
            text-align: left;
        }

        table.results td {
            border: 1px solid #cbd5e1;
            padding: 8px;
            font-size: 11px;
        }

        tr:nth-child(even) {
            background: #f8fafc;
        }

        .footer {
            margin-top: 25px;
            text-align: right;
            font-size: 10px;
            color: #64748b;
        }

        .status-submitted {
            color: green;
            font-weight: bold;
        }

        .status-pending {
            color: orange;
            font-weight: bold;
        }
    </style>
</head>
<body>

    <!-- Header -->
    <div class="header">
        <h1>Exam Results Report</h1>
        <p>Enterprise Examination Management System</p>
        <p>Generated on: {{ now()->format('d M Y h:i A') }}</p>
    </div>

    <!-- Summary Stats -->
    <table class="stats">
        <tr>
            <td>
                <span class="stats-label">Total Attempts</span>
                {{ $results->count() }}
            </td>

            <td>
                <span class="stats-label">Average Score</span>
                {{ number_format($results->avg('score'), 2) }}
            </td>

            <td>
                <span class="stats-label">Completed</span>
                {{ $results->where('status', 'submitted')->count() }}
            </td>
        </tr>
    </table>

    <!-- Main Results Table -->
    <table class="results">
        <thead>
            <tr>
                <th>#</th>
                <th>Student</th>
                <th>Exam</th>
                <th>Score</th>
                <th>Status</th>
                <th>Submitted At</th>
            </tr>
        </thead>

        <tbody>
            @forelse($results as $index => $result)
                <tr>
                    <td>{{ $index + 1 }}</td>
                    <td>{{ $result->student->name }}</td>
                    <td>{{ $result->exam->title }}</td>
                    <td>{{ $result->score }}</td>

                    <td class="{{ $result->status === 'submitted'
                        ? 'status-submitted'
                        : 'status-pending' }}">
                        {{ ucfirst($result->status) }}
                    </td>

                    <td>
                        {{ optional($result->submitted_at)->format('d M Y h:i A') }}
                    </td>
                </tr>
            @empty
                <tr>
                    <td colspan="6" style="text-align:center;">
                        No results available.
                    </td>
                </tr>
            @endforelse
        </tbody>
    </table>

    <!-- Footer -->
    <div class="footer">
        Generated by EMS Pro • Confidential Report
    </div>

</body>
</html>
